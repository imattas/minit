[unit]
name = "network.service"
description = "Alpine minirootfs baseline network placeholder"
kind = "service"

[exec]
start = ["/bin/sleep", "600"]

[restart]
policy = "on-failure"
limit = "3/min"
backoff = "fixed"

[security]
no_new_privileges = true
